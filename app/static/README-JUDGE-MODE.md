# CODEZILLA SIH Judge Mode

Open `/static/judge.html` while the FastAPI service is running.

Use **RUN 7-SCENARIO DEMO** for the deterministic presentation-safe replay. Use the normal `/` dashboard for live passive capture.

Reconnaissance and Data Exfiltration are transparent behavioral-policy detectors; the other four production detectors use bundled ML models. The project does not send probes, inject packets, decrypt TLS/QUIC payloads, or issue mitigation commands across the one-way ingest path.
